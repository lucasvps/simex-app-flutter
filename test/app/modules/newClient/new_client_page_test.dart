import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('NewClientPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewClientPage(title: 'NewClient')));
    //  final titleFinder = find.text('NewClient');
    //  expect(titleFinder, findsOneWidget);
  });
}
