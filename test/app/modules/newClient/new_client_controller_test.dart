import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:simex_app/app/modules/newClient/new_client_module.dart';

void main() {
  initModule(NewClientModule());
  // NewClientController newclient;
  //
  setUp(() {
    //     newclient = NewClientModule.to.get<NewClientController>();
  });

  group('NewClientController Test', () {
    //   test("First Test", () {
    //     expect(newclient, isInstanceOf<NewClientController>());
    //   });

    //   test("Set Value", () {
    //     expect(newclient.value, equals(0));
    //     newclient.increment();
    //     expect(newclient.value, equals(1));
    //   });
  });
}
