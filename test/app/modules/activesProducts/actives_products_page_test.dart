import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:simex_app/app/modules/activesProducts/actives_products_page.dart';

void main() {
  testWidgets('ActivesProductsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ActivesProductsPage(title: 'ActivesProducts')));
    //  final titleFinder = find.text('ActivesProducts');
    //  expect(titleFinder, findsOneWidget);
  });
}
