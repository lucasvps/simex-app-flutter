import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('NewRegisterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewRegisterPage(title: 'NewRegister')));
    //  final titleFinder = find.text('NewRegister');
    //  expect(titleFinder, findsOneWidget);
  });
}
