import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:simex_app/app/modules/newRegister/new_register_module.dart';

void main() {
  initModule(NewRegisterModule());
  // NewRegisterController newregister;
  //
  setUp(() {
    //     newregister = NewRegisterModule.to.get<NewRegisterController>();
  });

  group('NewRegisterController Test', () {
    //   test("First Test", () {
    //     expect(newregister, isInstanceOf<NewRegisterController>());
    //   });

    //   test("Set Value", () {
    //     expect(newregister.value, equals(0));
    //     newregister.increment();
    //     expect(newregister.value, equals(1));
    //   });
  });
}
