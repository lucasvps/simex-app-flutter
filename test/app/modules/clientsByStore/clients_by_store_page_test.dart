import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:simex_app/app/modules/clientsByStore/clients_by_store_page.dart';

void main() {
  testWidgets('ClientsByStorePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ClientsByStorePage(title: 'ClientsByStore')));
    //  final titleFinder = find.text('ClientsByStore');
    //  expect(titleFinder, findsOneWidget);
  });
}
