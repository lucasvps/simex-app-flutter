import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:simex_app/app/modules/clientsByStore/clients_by_store_controller.dart';
import 'package:simex_app/app/modules/clientsByStore/clients_by_store_module.dart';

void main() {
  initModule(ClientsByStoreModule());
  // ClientsByStoreController clientsbystore;
  //
  setUp(() {
    //     clientsbystore = ClientsByStoreModule.to.get<ClientsByStoreController>();
  });

  group('ClientsByStoreController Test', () {
    //   test("First Test", () {
    //     expect(clientsbystore, isInstanceOf<ClientsByStoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(clientsbystore.value, equals(0));
    //     clientsbystore.increment();
    //     expect(clientsbystore.value, equals(1));
    //   });
  });
}
