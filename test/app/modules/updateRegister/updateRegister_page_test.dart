import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('UpdateRegisterPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UpdateRegisterPage(title: 'UpdateRegister')));
    //  final titleFinder = find.text('UpdateRegister');
    //  expect(titleFinder, findsOneWidget);
  });
}
