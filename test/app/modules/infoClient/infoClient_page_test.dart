import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:simex_app/app/modules/infoClient/infoClient_page.dart';

main() {
  testWidgets('InfoClientPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InfoClientPage(title: 'InfoClient')));
    //  final titleFinder = find.text('InfoClient');
    //  expect(titleFinder, findsOneWidget);
  });
}
