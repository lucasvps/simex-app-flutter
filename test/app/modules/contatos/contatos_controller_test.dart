import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:simex_app/app/modules/contatos/contatos_controller.dart';
import 'package:simex_app/app/modules/contatos/contatos_module.dart';

void main() {
  initModule(ContatosModule());
  // ContatosController contatos;
  //
  setUp(() {
    //     contatos = ContatosModule.to.get<ContatosController>();
  });

  group('ContatosController Test', () {
    //   test("First Test", () {
    //     expect(contatos, isInstanceOf<ContatosController>());
    //   });

    //   test("Set Value", () {
    //     expect(contatos.value, equals(0));
    //     contatos.increment();
    //     expect(contatos.value, equals(1));
    //   });
  });
}
