import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('ContatosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContatosPage(title: 'Contatos')));
    //  final titleFinder = find.text('Contatos');
    //  expect(titleFinder, findsOneWidget);
  });
}
