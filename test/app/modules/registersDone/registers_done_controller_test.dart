import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:simex_app/app/modules/registersDone/registers_done_controller.dart';
import 'package:simex_app/app/modules/registersDone/registers_done_module.dart';

void main() {
  initModule(RegistersDoneModule());
  // RegistersDoneController registersdone;
  //
  setUp(() {
    //     registersdone = RegistersDoneModule.to.get<RegistersDoneController>();
  });

  group('RegistersDoneController Test', () {
    //   test("First Test", () {
    //     expect(registersdone, isInstanceOf<RegistersDoneController>());
    //   });

    //   test("Set Value", () {
    //     expect(registersdone.value, equals(0));
    //     registersdone.increment();
    //     expect(registersdone.value, equals(1));
    //   });
  });
}
